export default class Release {
  constructor(guid, series, episode, resolution, publication) {
    this.guid = guid;
    this.series = series;
    this.episode = episode;
    this.resolution = resolution;
    this.publication = publication;
  }
}
